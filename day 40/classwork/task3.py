numbers = "1 2 3 4 5 7"
split = numbers.split()  # Ahem, Split function Splits the numbers, and deletes any unececcary spaces.

numbers = numbers [::-1]

"Hold on".join() # With the join function you, can make letters and numbers fuse together, without any spaces.

text = "omor"
print(text.replace("omor", "2")) # replace functions with the diffrient text

text2 = "1           3"
print(len(text2.strip())) # this one strips a specific Thing from the tag